name: Daily Auto Shorts with Multi-Platform Upload

on:
  schedule:
    - cron: "50 4 * * *"
    - cron: "15 10 * * *"
    - cron: "29 16 * * *"
    - cron: "1 21 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-and-upload:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
        cache: 'pip'

    - name: Install FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Install Korean fonts
      run: |
        sudo mkdir -p /usr/share/fonts/truetype/noto-kr
        sudo cp fonts/*.otf /usr/share/fonts/truetype/noto-kr/
        sudo fc-cache -f
        fc-list | grep -i "Noto Sans KR" || true

    - name: Cache Vosk Model
      uses: actions/cache@v4
      with:
        path: vosk-model-small-ko-0.22
        key: vosk-model-small-ko-0.22

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    - name: Check and generate topics if needed
      env:
        POLLINATIONS_API_KEY: ${{ secrets.POLLINATIONS_API_KEY }}
      run: |
        python generate_topics.py

    - name: Generate daily short video
      env:
        POLLINATIONS_API_KEY: ${{ secrets.POLLINATIONS_API_KEY }}
        GOOGLE_SERVICE_ACCOUNT_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_KEY }}
        GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }}
      run: |
        python main.py

    - name: Upload to all platforms
      env:
        POLLINATIONS_API_KEY: ${{ secrets.POLLINATIONS_API_KEY }}
        YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
        YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
        YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN }}
        IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
        IG_USER_ID: ${{ secrets.IG_USER_ID }}
        TIKTOK_ACCESS_TOKEN: ${{ secrets.TIKTOK_ACCESS_TOKEN }}
        FB_ACCESS_TOKEN: ${{ secrets.FB_ACCESS_TOKEN }}
        FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
        THREADS_ACCESS_TOKEN: ${{ secrets.THREADS_ACCESS_TOKEN }}
        THREADS_USER_ID: ${{ secrets.THREADS_USER_ID }}
        TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
        TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
        TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
        TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}
        VK_ACCESS_TOKEN: ${{ secrets.VK_ACCESS_TOKEN }}
        VK_GROUP_ID: ${{ secrets.VK_GROUP_ID }}
      run: |
        python upload_all_platforms.py

    - name: Commit updated topics and tracking files
      if: always()
      run: |
        set +e
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        # Never commit files that contain merge-conflict markers
        if grep -rlE '^(<<<<<<<|=======|>>>>>>>)' topics.txt used_topics.txt used_images.json output/ 2>/dev/null; then
          echo "ERROR: conflict markers found, refusing to commit"
          exit 1
        fi
        # Drop any invalid JSON tracking file so it is not committed broken
        python -c "import json,os; f='used_images.json';
if os.path.exists(f):
 try: json.load(open(f,encoding='utf-8'))
 except Exception: os.remove(f)" 2>/dev/null || true
        git add topics.txt used_topics.txt used_images.json output/story_en.txt 2>/dev/null
        # Rebase with autostash; abort on conflict instead of leaving markers
        git pull --rebase --autostash origin main || { git rebase --abort 2>/dev/null; echo "WARNING: rebase failed"; }
        # Un-stage any file that ended up with conflict markers
        git diff --cached --name-only | while read -r f; do
          if [ -f "$f" ] && grep -qE '^(<<<<<<<|=======|>>>>>>>)' "$f" 2>/dev/null; then
            echo "WARNING: un-staging conflict file $f"
            git reset -q -- "$f"
          fi
        done
        if ! git diff --cached --quiet; then
          git commit -m "Auto-update: track used topics and images [skip ci]"
        else
          echo "No changes to commit"
        fi
        git push || { sleep 5; git push; }
        exit 0

